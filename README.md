# Frontend End Semester Exam (2400030097)

This repository contains a Vite + React frontend project demonstrating reusable UI primitives across multiple pages.

## Available scripts

- `npm install` — install dependencies
- `npm run dev` — start local development server
- `npm run build` — create production build
- `npm run lint` — run ESLint checks
